/**
 * Redaction pattern notes (logic lives in background.js page-injected func).
 * - emails, phones, card-like digit runs, SSN, JWT, api/token strings
 * - password/email/cc inputs always covered
 */
